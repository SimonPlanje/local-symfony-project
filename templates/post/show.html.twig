{% extends 'base.html.twig' %}

{% block title %}Hello Show!{% endblock %}

{% block body %}
<h1>{{ post.title }}</h1>
<p>{{ post.description }}</p>
<p><a href="{{ path('author_show', {'id': post.author.id}) }}">{{ post.author }}</a></p>
{% endblock %}
