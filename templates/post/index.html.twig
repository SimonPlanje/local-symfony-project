{% extends 'base.html.twig' %}

{% block title %}Hello PostController!{% endblock %}

{% block body %}
    <ul>
    {% for post in posts %}
        <li>{{ post.id ~ ' ' ~ post.title }}</li>
    {% endfor %}
    </ul>
</div>
{% endblock %}
