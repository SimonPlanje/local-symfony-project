{{ form_start(form) }}

{{ form_row(form.title) }}
{{ form_row(form.description) }}
{{ form_row(form.author) }}
{{ form_row(form.tags) }}
{{ form_row(form.visible) }}

<button type="submit" class="btn">{{ button_label|default('Opslaan') }}</button>

{{ form_end(form) }}